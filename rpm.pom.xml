<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <packaging>rpm</packaging>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>sod_01</artifactId>
  <version>${revision}${sha1}${changelist}.el${env.OS_VERSION}</version>
  <name>sod_01</name>
  <description>sod_01</description>
  <groupId>com.teragrep</groupId>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <revision>0.0.1</revision>
    <changelist>-SNAPSHOT</changelist>
    <sha1/>
  </properties>
  <build>
    <directory>${project.basedir}/target</directory>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>rpm-maven-plugin</artifactId>
        <version>2.2.0</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>default-rpm</id>
            <goals>
              <goal>rpm</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
        <configuration>
          <summary>${project.groupId}-${project.artifactId}</summary>
          <name>${project.groupId}-${project.artifactId}</name>
          <version>${project.version}</version>
          <release>${env.BUILD_ID}</release>
          <license>Proprietary</license>
          <distribution>teragrep Log Management Suite</distribution>
          <vendor>https://teragrep.com/</vendor>
          <packager>teragrep &lt;servicedesk@teragrep.com&gt;</packager>
          <group>teragrep/LogManagementSuite</group>
          <needarch>true</needarch>
          <description>${project.groupId}-${project.artifactId} based on libsodium ${env.LIBSODIUM_VERSION}</description>
          <defaultUsername>root</defaultUsername>
          <defaultGroupname>root</defaultGroupname>
          <defaultFilemode>0644</defaultFilemode>
          <defaultDirmode>0755</defaultDirmode>
          <keyname>${env.RPM_SIGNING_KEYNAME}</keyname>
          <keyPassphrase>
            <passphrase>${env.RPM_SIGNING_PASSPHRASE}</passphrase>
          </keyPassphrase>
          <defineStatements>
            <defineStatement>_build_id_links none</defineStatement>
            <defineStatement>__provides_exclude ^osgi\\(.*$</defineStatement>
            <defineStatement>__requires_exclude ^osgi\\(.*$</defineStatement>
          </defineStatements>
          <mappings>
            <!-- lib -->
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/lib</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>755</filemode>
              <dirmode>755</dirmode>
              <username>root</username>
              <groupname>root</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/release/opt/teragrep/${project.artifactId}/lib</location>
                  <excludes>
                    <!-- brp-remove-la-files will remove these and fail the build unless manually handled otherwise -->
                    <exclude>**/*.la</exclude>
                  </excludes>
                </source>
              </sources>
            </mapping>
            <!-- include -->
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/include</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>644</filemode>
              <dirmode>644</dirmode>
              <username>root</username>
              <groupname>root</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/release/opt/teragrep/${project.artifactId}/include</location>
                </source>
              </sources>
            </mapping>
          </mappings>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
